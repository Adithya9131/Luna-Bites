/* General Page Styling */
body {
    font-family: Arial, sans-serif;
    background-color: #fdf6e3;  /* Warm background color */
    text-align: center;
    margin: 0;
    padding: 0;
}

/* Navbar Styling */
nav {
    display: flex;
    justify-content: space-between; /* Space between logo and nav links */
    align-items: center; /* Vertically align items */
    padding: 15px 20px;
    background-color: #2C2C54;
}

/* Centering Logo */
.logo {
    display: flex;
    justify-content: center; /* Center horizontally */
    align-items: center;
    flex-grow: 1; /* Allows the logo to take center space */
}

.logo img {
    max-width: 200px;
    height: auto;
    display: block;
}

/* Navbar Links */
.nav-links {
    list-style: none;
    display: flex;
    gap: 15px;
    margin-right: 20px; /* Push links to right */
}

.nav-links li {
    display: inline-block;
}

/* Button Styling */
.nav-links a {
    text-decoration: none;
    color: black;
    padding: 10px 15px;
    border-radius: 5px;
    background-color: white; /* White background */
    font-weight: bold;
    transition: 0.3s;
}

.nav-links a:hover {
    background-color: #ddd; /* Light grey on hover */
    color: blue;
}

/* About Us Page */
.ceo-message, .legacy {
    text-align: center;
    max-width: 800px;
    margin: 50px auto;
    padding: 20px;
    background-color: #f4f4f4;
    border-radius: 10px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
}

.ceo-message h2, .legacy h2 {
    font-size: 28px;
    color: #d2691e;
    margin-bottom: 15px;
}

.ceo-message p, .legacy p {
    font-size: 18px;
    line-height: 1.6;
    color: #333;
    margin-bottom: 15px;
}

.ceo-img {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    object-fit: cover;
    display: block;
    margin: 0 auto 20px;
}
